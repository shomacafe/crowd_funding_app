require 'rails_helper'

RSpec.describe "Returns", type: :request do
  # let!(:user) { create(:user) }
  # let!(:project) { create(:project) }
  # let!(:return) { create(:return) }

  # before do
  #   sign_in user
  # end


  # describe "プロジェクト詳細表示機能" do
  #   before do
  #     get project_path(project.id)
  #   end

  #   it "200レスポンスを返すこと" do
  #     expect(response).to have_http_status(200)
  #   end

  #   it "responseにリターン名が含まれること" do
  #     @return = create(:return)
  #     expect(response.body).to include(@return.return_name)
  #   end
  # end

end
